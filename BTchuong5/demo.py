# Các hàm trong Pyhthon:
#     Sort:
#         - Sắp xếp chữ thường trước chữ hoa: L.sort(key=str.lower) ngược lại L.sort(key=str.upper)
#         - Sắp xếp giảm dần: L.sort(reverse=True)
# Reverse(): Đảo ngược thứ tự các phần tử trong list
#count(x): Đếm số phần tử x xuất hiện trong list
#Xoá phần tử trong list: 
# - del(L[i]): phải biết số index i
# - L.remove(x): Xoá phần tử đầu tiên tìm thấy
# - L.pop(i): Xoá phần tử có số chỉ mục i